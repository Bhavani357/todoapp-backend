##
* npm init -y
* copy paste the all code inside package.json 
* npm install
* copy paste all folders and files data except node_modules and package-lock.json (these are automatically creates)
* npm run dev
